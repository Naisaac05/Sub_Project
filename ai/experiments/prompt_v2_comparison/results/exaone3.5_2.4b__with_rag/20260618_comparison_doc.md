---
type: experiment
category: rag
status: active
updated: 2026-06-18
description: "v1 vs v2 프롬프트 비교 결과 (RAG 포함) 모델 성능 평가 및 POC 실험 결과 분석"

---

# v1 vs v2 프롬프트 비교 결과 (RAG 포함)

- 모델: `exaone3.5:2.4b`
- 샘플 수: 5
- 생성 시각: 2026-05-27 17:49:33
- RAG 포함: 검색 (limit=3, score≥5.0) → 프롬프트 컨텍스트 주입

## 평가 방법
각 샘플별로 v1/v2 출력을 나란히 보고, 아래 기준으로 직접 채점하세요:
- **자연스러움**: 한국어 톤이 자연스러운가? (영어 단어 섞임/번역체 없음)
- **진단 적합성**: 학습자의 상태에 맞는 꼬리질문인가?
- **형식 준수**: 3문장 이내 + 물음표로 끝남
- **컨텍스트 활용**: RAG 카드 내용을 답변에 잘 녹였는가?

---

## 샘플 followup-001-dont-know — 학습자가 모른다고 답함

**문제**: JPA의 N+1 문제가 무엇인가요?
**학습자 답변**: 모르겠어요

**RAG 검색 결과**:
- ✅ `spring-n-plus-one` (N+1 문제) — score: 20.5
- ✅ `spring-fetch-join` (Fetch Join) — score: 15.0
- ⚠️ 점수미달 `auto-review-hashcode` (hashCode) — score: 1.0

### v1 출력 (26149ms)
```
N+1 문제는 주로 여러 개의 엔티티를 조회할 때 첫 번째 엔티티 이후 각 엔티티의 관련 데이터를 별도로 요청하여 발생하는 성능 저하 현상입니다. 따라서 올바른 정의는 **1번** 옵션인 "연관 엔티티 조회 시 추가 쿼리가 N번 더 발생하는 현상"입니다.

다음 질문은 무엇일까요?
- 학습자가 이해한 후, 실제 코드 예시나 특정 상황에서 이 문제를 어떻게 해결할 수 있는지 설명해 보세요?
```

### v2 출력 (51101ms)
```
학습자의 답변을 바탕으로 다음 단계를 안내하는 꼬리질문입니다:

학습자님께서 "엔티티가 N+1개 저장되는 현상"이라고 답하시면서 이해에 어려움을 느끼셨네요. N+1 문제는 실제로 연관된 데이터들이 여러 번 요청될 때마다 데이터베이스에서 추가 쿼리를 통해 더 많은 엔티티 정보를 가져오게 되는 상황을 말합니다. 특히, 첫 번째 엔티티를 가져온 후 그 엔티티와 연결된 다른 엔티티들까지 함께 불러오는데, 이 과정이 반복되면서 성능 저하가 발생하죠. 맞으셨지만, 좀 더 구체적으로 연관 엔티티의 로딩 방식과 그로 인한 추가 쿼리 문제에 대해 생각해보시는 건 어떨까요? 예를 들어, 첫 번째 엔티티를 가져온 후 그와 관련된 모든 데이터를 한 번에 가져오도록 설정하면 어떻게 될까요?
```

### 채점 (직접 작성)
- 자연스러움: v1 _/5, v2 _/5
- 진단 적합성: v1 _/5, v2 _/5
- 컨텍스트 활용: v1 _/5, v2 _/5
- 메모: 

---

## 샘플 followup-002-partial — 부분적으로 맞음

**문제**: 리스트와 튜플의 가장 큰 차이는 무엇인가요?
**학습자 답변**: 튜플이 리스트보다 빠르다고 들어서 속도 차이라고 골랐어요

**RAG 검색 결과**:
- ⚠️ 점수미달 `auto-review-hashcode` (hashCode) — score: 1.0

### v1 출력 (14715ms)
```
가변성 차이를 이해하려면, 리스트와 튜플의 주요 차이점 중 하나가 중요합니다. 튜플은 한 번 생성되면 값이 변경될 수 없는 반면, 리스트는 요소 추가나 삭제가 가능해 동적으로 변화할 수 있습니다. 따라서 올바른 선택은 **1. 가변성 차이**입니다. 다음으로, 어떤 상황에서 이러한 특성 차이를 고려해야 할지 생각해 보세요?
```

### v2 출력 (33870ms)
```
학습자님께서 "속도 차이"를 선택하신 것은 이해합니다. 실제로 리스트와 튜플의 가장 핵심적인 차이점 중 하나가 바로 **변경 가능성**이라는 점을 고려해보는 건 어떨까요? 속도 차이도 중요한 특징이지만, 데이터 구조 자체가 어떻게 변할 수 있는지에 대한 이해가 더 깊어질 수 있습니다. 그럼 다음으로, 튜플은 한 번 생성되면 내용을 변경할 수 없다는 사실에 대해 생각해볼까요? 이 점이 가변성 차이와 어떻게 연결되는지 고민해보시는 건 어떨까요?

**꼬리질문:** 튜플의 불변성이 데이터 구조 선택 시 어떤 영향을 미칠지 생각해보세요!
```

### 채점 (직접 작성)
- 자연스러움: v1 _/5, v2 _/5
- 진단 적합성: v1 _/5, v2 _/5
- 컨텍스트 활용: v1 _/5, v2 _/5
- 메모: 

---

## 샘플 followup-003-wrong-concept — 잘못된 개념 적용

**문제**: equals를 재정의할 때 함께 재정의해야 하는 것은?
**학습자 답변**: 객체를 문자열로 표현할 때 같이 쓰니까 toString도 같이 재정의해야 한다고 생각했어요

**RAG 검색 결과**:
- ✅ `java-equals` (equals) — score: 10.5
- ✅ `auto-review-hashcode` (hashCode) — score: 7.5
- ⚠️ 점수미달 `spring-fetch-join` (Fetch Join) — score: 3.0

### v1 출력 (24863ms)
```
정답은 hashCode입니다. equals를 재정의할 때는 객체의 고유 식별자로 사용되는 hashCode도 함께 고려해야 합니다. toString과 비교해보면, toString은 주로 사용자 친화적인 문자열 표현을 위해 사용됩니다. 따라서 equals와 직접적으로 연관성이 적습니다.

다음 질문: equals 메서드 외에 객체 간의 논리적 동등성을 확인할 때 자주 사용되는 다른 메서드는 무엇인가요?
```

### v2 출력 (52999ms)
```
학습자님의 답변과 평가 결과를 바탕으로 다음 단계로 안내해 드리겠습니다.

학습자님은 `equals` 메서드 재정의와 관련하여 `toString`, `hashCode` 중에서 특히 `toString`을 선택하셨는데, 이는 객체 표현 시 주로 사용되며 `equals`와 직접적인 연관성이 덜 강조되었네요. 핵심 개념으로는 `equals`와 함께 `hashCode`가 밀접하게 연결되어 있다는 점을 다시 한번 강조하고 싶습니다. 특히 객체의 동등성 판단과 해시 테이블에서의 효율적인 검색에 `hashCode`가 필수적이라는 점을 이해하시면 좋겠네요.

그럼, 다음 질문으로 넘어가 보실까요?
- `equals` 메서드를 재정의할 때 함께 고려해야 할 다른 핵심 개념은 무엇일지 생각해보시는 건 어떨까요? 특히 객체 간 비교와 관련된 부분에 초점을 맞춰 보시면 도움이 될 것 같습니다.
```

### 채점 (직접 작성)
- 자연스러움: v1 _/5, v2 _/5
- 진단 적합성: v1 _/5, v2 _/5
- 컨텍스트 활용: v1 _/5, v2 _/5
- 메모: 

---

## 샘플 followup-004-confident-wrong — 자신감 있게 틀린 답

**문제**: fetch join을 컬렉션에 사용할 때 가장 주의해야 할 점은?
**학습자 답변**: fetch join을 쓰면 데이터베이스에서 정렬되니까 결과 순서가 보장된다고 확신해요

**RAG 검색 결과**:
- ✅ `spring-fetch-join` (Fetch Join) — score: 11.0
- ⚠️ 점수미달 `spring-n-plus-one` (N+1 문제) — score: 2.0

### v1 출력 (21872ms)
```
fetch join 시 주의해야 할 주요 점은 페이징 처리 방식입니다. 메모리에서 직접 데이터를 처리하기 때문에 큰 데이터셋에서 성능 저하가 발생할 수 있습니다. 또한, 모든 연관 관계를 fetch join으로 설정하면 데이터베이스 부하와 메모리 사용량이 증가할 수 있다는 점도 기억하세요.

다음 질문: 어떤 상황에서 N+1 문제가 발생할 가능성이 높아지는지 설명해 주실까요?
```

### v2 출력 (38256ms)
```
학습자의 답변을 바탕으로 다음 단계를 안내하는 꼬리질문을 드리겠습니다:

학습자가 "조인 결과가 항상 정렬된다고 확신해요"라고 답했지만, 페이징과 메모리 처리에 대한 이해가 부족한 것 같아요. 조인 연산 자체로 인해 데이터 순서가 보장되지 않고, 실제로는 데이터베이스의 인덱스와 쿼리 실행 방식에 따라 페이징이 어떻게 이루어지는지 고려해야 합니다. 그럼 다음으로, **fetch join을 사용할 때 실제 데이터베이스 환경에서 어떤 문제들이 발생할 수 있으며, 이를 해결하기 위한 방법은 무엇인지 고민해 볼 수 있을까요?**
```

### 채점 (직접 작성)
- 자연스러움: v1 _/5, v2 _/5
- 진단 적합성: v1 _/5, v2 _/5
- 컨텍스트 활용: v1 _/5, v2 _/5
- 메모: 

---

## 샘플 followup-005-second-step — 두 번째 꼬리질문 단계

**문제**: @ControllerAdvice의 주요 역할은?
**학습자 답변**: 이전 꼬리질문에서 예외를 한 곳에 모아 처리한다는 건 이해했어요. 그런데 @ExceptionHandler랑 어떻게 다른지 헷갈려요

**RAG 검색 결과**:
- ✅ `java-backend-controlleradvice` (@ControllerAdvice) — score: 16.0
- ⚠️ 점수미달 `auto-review-hashcode` (hashCode) — score: 1.0
- ⚠️ 점수미달 `auto-review-recyclerview` (RecyclerView) — score: 1.0

### v1 출력 (22181ms)
```
@ControllerAdvice는 여러 컨트롤러에서 공유되는 예외 처리 로직을 중앙 집중적으로 관리하는 기능입니다. 특히 전역 예외 처리를 통해 일관된 응답 형식을 유지하는데 효과적입니다. @ExceptionHandler와 혼동이 있으셨는데, 이는 특정 컨트롤러 내에서 개별 예외를 처리하기 위한 어노테이션인 반면, @ControllerAdvice는 더 넓은 범위의 공통 예외 관리에 초점을 맞춥니다.

다음 질문: `@ControllerAdvice`를 사용할 때 주의해야 할 주요 사항이나 실제 적용 사례가 있다면 설명해 주실 수 있나요?
```

### v2 출력 (44570ms)
```
학습자님의 답변을 보니, 의존성 주입이라는 개념에 대해 잘 이해하고 계신 것 같아요! 특히 예외 처리와 관련된 부분에서 혼동을 느끼고 계시다는 점은 공감됩니다. `@ControllerAdvice`가 다양한 컨트롤러에서 공통적으로 사용되는 예외 처리를 중앙 집중화하는 역할을 한다는 점을 다시 한번 강조해보면 어떨까요?

특히 `@ExceptionHandler`와의 차이점에 대해 생각해볼 때, `@ControllerAdvice`가 어떤 상황에서 더 유용하게 활용될 수 있는지 고민해 보시는 건 어떨까요? 예를 들어, 여러 컨트롤러에서 일관된 예외 응답 방식을 유지하고자 할 때 어떻게 적용할 수 있을지 상상해보시면 도움이 될 것 같아요.

그럼 이렇게 생각해보실래요? `@ControllerAdvice`가 실제 프로젝트에서 어떤 이점으로 활용될 수 있을지 고민해보세요! 🤔
```

### 채점 (직접 작성)
- 자연스러움: v1 _/5, v2 _/5
- 진단 적합성: v1 _/5, v2 _/5
- 컨텍스트 활용: v1 _/5, v2 _/5
- 메모: 

---
